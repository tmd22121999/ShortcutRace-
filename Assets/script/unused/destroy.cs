using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class destroy : MonoBehaviour
{

    private void OnTriggerStay(Collider other)
    {
        //if( (!Input.GetMouseButton(0)) && (other.tag=="other") )
          //Destroy(other.gameObject);
          
    }
}
